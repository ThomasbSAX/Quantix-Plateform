// JS dédié à la plateforme saphir
// Ajoute ici les fonctions spécifiques pour la plateforme Saphir

document.addEventListener('DOMContentLoaded', () => {
    const saphirBtn = document.getElementById('saphir-btn');
    if(saphirBtn){
        saphirBtn.addEventListener('click', async () => {
            // Ajoute ici la logique d'appel à l'API Saphir
            // ...
        });
    }
});
