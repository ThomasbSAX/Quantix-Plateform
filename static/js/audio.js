// JS dédié à la plateforme audio
// Ajoute ici les fonctions spécifiques pour l'upload, la transcription, etc.

// Exemple :
document.addEventListener('DOMContentLoaded', () => {
    const transcribeBtn = document.getElementById('transcribe-btn');
    if(transcribeBtn){
        transcribeBtn.addEventListener('click', async () => {
            // Ajoute ici la logique d'appel à l'API de transcription
            // ...
        });
    }
});
